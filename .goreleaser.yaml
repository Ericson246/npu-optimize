version: 2

before:
  hooks:
    - go mod tidy

builds:
  - main: ./cmd/npu-optimize
    binary: npu-optimize
    ldflags:
      - -X github.com/Ericson246/npu-optimize/internal/constants.Version={{ .Version }}
      - -X github.com/Ericson246/npu-optimize/internal/constants.UserAgent=npu-optimize/{{ .Version }}
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64

archives:
  - format: tar.gz
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: "checksums.txt"

snapshot:
  name_template: "{{ .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^refactor:"
